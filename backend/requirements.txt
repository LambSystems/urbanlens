# Lightweight backend dependencies for hosted portfolio demo mode.
# Railpack auto-installs this file when backend/ is the service root.
#
# Full local ThermalGen inference dependencies live in requirements-full.txt.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
sse-starlette>=2.0,<3.0
Pillow>=10.0,<12.0
