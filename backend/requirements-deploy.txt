# Lightweight backend dependencies for hosted portfolio demo mode.
# This intentionally excludes ThermalGen/PyTorch/geospatial model dependencies.
#
# Use with:
#   DEMO_MODE=true
#   LLM_PROVIDER=mock

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
sse-starlette>=2.0,<3.0
Pillow>=10.0,<12.0
